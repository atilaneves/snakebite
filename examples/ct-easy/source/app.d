// The real corpus lives in corpus.d: dub's synthetic "unittest"
// configuration excludes this file (the main source file), so anything
// here would never run under `dub test`.

int main() {
    return 0;
}
