name "rt-cerealed-1"
description "as many real cerealed tests as we can do right now"
targetType "library"
targetPath "bin"
dflags "-checkaction=context"


configuration "unittest" {
    targetType "executable"
    targetName "ut"
    targetPath "bin"

    mainSourceFile "tests/main.d"
    excludedSourceFiles "source/main.d"
    sourcePaths "tests"
    importPaths "tests"
}
